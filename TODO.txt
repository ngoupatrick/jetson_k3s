TODO:
jetpack 4.6.6 k3s v1.24.17+k3s1
nodes M, S1, S2. S1 & S2 can talk to only M, and only can talk to myhost. all nodes have acces to internet
myhost(prometheus(federate+longterm storage)+grafana[storage+dashboard]+alerts [slack, Mail]) -> M(prometheus, collect metrics for S1 & S2 & M  + 2h storage) --> S1,S2 (jetson-exporter, collect metrics and send to M).
myhost(loki(federate+longterm storage)+grafana[storage+dashboard]+alerts [slack, Mail]) -> M(fluent-bit, collect logs for S1 & S2 & M  + 2h storage) --> S1,S2 (promtail, collect logs and send to M).
curl -sfL https://get.k3s.io | INSTALL_K3S_VERSION="v1.24.17+k3s1" sh -s - server \
  --cluster-init \
  --disable metrics-server \
  --node-ip=@IPM \
  --bind-address @IPM \
  --advertise-address @IPM \
  --data-dir /data \
  --etcd-snapshot-schedule-cron "0 */5 * * *" \
  --etcd-snapshot-retention 5 \
  --node-label "device-type=jetson-nano" \
  --node-label "jetpack=4.6.6" \
  --node-label "hardware=gpu" \
  --docker \
  --write-kubeconfig-mode 644

# dependency management
# https://deps.dev

use timm
tools to deply easily on k3s cluster
send notification when job is failed, running, completed states
send training and predictions results (via mail, ...)

use your phone/myhost camera stream 

use midiamtx as pull with OBS, not push

use stream in opencv code
  use case:
    Détection de mouvement (MOG2)
    Mesure de Télémétrie / Statistiques
    Sauvegarde Sélective (Snapshot)
    Envoi vers une base de données (InfluxDB / MQTT)

update alertmanager.yml
Promtail / Alloy / loki / grafana
https://blog.stephane-robert.info/docs/outils/observabilite/logs/loki/

pyspark using our k3s cluster





